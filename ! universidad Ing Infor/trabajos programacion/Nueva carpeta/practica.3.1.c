#include "simpleio.h"

int entero n;
int mayor;
int menor;

// Bucle para calcular mayor o menor de los numeros

int main(void)  {
	escribir_texto("cantidad de numeros: ");
	leer_entero(n);
	leer_entero(numero);
	mayor = numero  menor = numero
	mientras que (n > 1) hacer
	 leer_entero(numero);
	 si (numero > mayor) entonces
	   mayor = numero;
	  sino si (numero < menor) entonces
	   menor = numero;
fmq;
}
   escribir_texto("Mayor: "); escribir_entero("mayor: ")
   escribir_texto("Menor: "); escribir_entero("menor: ")
   
   
	  
